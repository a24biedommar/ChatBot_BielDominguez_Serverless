# Prompts utilitzats per l'usuari (Historial de Conversa)

A continuació es detallen totes les peticions, ordres i prompts que m'has indicat durant aquesta sessió per a depurar i afegir funcionalitats a la teva aplicació de Xatbot (Oni-chan):

### 1. Problema de connexió inicial (Error 503)
> "Al aixecar el docker i enviar-li un missatge al chat aquest hem retorna error 503, perque?"

### 2. Dubtes sobre Supabase i IndexedDB
> "tengo vacia el nuxt public supabase anon key como la consigo?"
> "es necesasrio el nuxt public supabase anon key??"
> "realment la meva aplicació utilitza indeexDB no supabase"

### 3. Persistència d'errors al Xat
> "Si le mando un mensaje a el chat me devuelve esta respuesta:
Error: [POST] \"http://localhost:3001/api/chat\": 503 Service Unavailable"

### 4. Error 500 Intern del Servidor
> "Error: [POST] \"http://localhost:3001/api/chat\": 500 Internal Server Error
ahora me devuelve este error porque? corrijelo"

### 5. Actualització del model de Gemini a 2.5
> "Error: [GoogleGenerativeAI Error]: Error fetching from https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent: [404 Not Found] models/gemini-1.5-flash is not found for API version v1beta, or is not supported for generateContent. Call ListModels to see the list of available models and their supported methods.
Me sale este error, i esto me sale porque tenemos que usar el modelo 2.5 flash de gemini gemini-2.5-flash"

### 6. Afegir funcionalitat visual de càrrega (UI)
> "Quiero que mientras el chat piensa que decir que aparezcan los tres ... como pantalla de carga en justo su mensaje (como hace chatgpt)"

### 7. Documentació del procés
> "añademe en Process.md los prompts utilizados"
> "quiero que en process.md esten los prompt que jo te he mandado a ti"
